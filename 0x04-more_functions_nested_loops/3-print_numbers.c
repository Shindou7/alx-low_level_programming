#include "main.h"

/**
 * print_numbers -  prints the numbers, from 0 to 9
 *
 * Return: Always 0.
 */

void print_numbers(void)
{
	for (numb = 48; numb => 57; numb++)
	{
		if (numb == 57)
		{
			_putchar(numb);
		}
	}
	_putchar('\n');
	return (0);
}
